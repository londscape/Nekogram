name: Build

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    env:
      TG_API_ID: ${{ secrets.TELEGRAM_API_ID || secrets.API_ID }}
      TG_API_HASH: ${{ secrets.TELEGRAM_API_HASH || secrets.API_HASH }}
      GRADLE_OPTS: -Dorg.gradle.jvmargs=-Xmx7g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8 -XX:+UseParallelGC

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android packages
        shell: bash
        run: |
          sdkmanager --install "platforms;android-36" "build-tools;36.1.0" "ndk;27.3.13750724" "cmake;4.0.3"

      - name: Configure Gradle memory
        shell: bash
        run: |
          mkdir -p ~/.gradle
          cat > ~/.gradle/gradle.properties <<EOF
          org.gradle.jvmargs=-Xmx7g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8 -XX:+UseParallelGC
          org.gradle.workers.max=1
          org.gradle.parallel=false
          org.gradle.daemon=false
          kotlin.daemon.jvmargs=-Xmx2g
          EOF

      - name: Validate Telegram API secrets
        shell: bash
        run: |
          test -n "$TG_API_ID" || (echo "::error::Set TELEGRAM_API_ID or API_ID in repository secrets" && exit 1)
          test -n "$TG_API_HASH" || (echo "::error::Set TELEGRAM_API_HASH or API_HASH in repository secrets" && exit 1)

      - name: Create local.properties
        shell: bash
        run: |
          cat > local.properties <<EOF
          sdk.dir=${ANDROID_HOME}
          apiId=${TG_API_ID}
          apiHash=${TG_API_HASH}
          mapsApiKey=
          sentryDsn=
          EOF

      - name: Build arm64 debug APK
        run: ./gradlew :TMessagesProj_App:assembleDebug --no-daemon --max-workers=1

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: nekogram-tg-ws-proxy-arm64-debug
          path: TMessagesProj_App/build/outputs/apk/debug/*.apk
